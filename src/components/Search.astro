---
const res = await fetch("/search.json")
  .then((r) => r.json())
  .catch(() => [])
---

<div class="relative">
  <input
    id="q"
    placeholder="Buscar…"
    class="w-full bg-transparent border border-[--color-border] rounded-xl px-4 py-2 outline-none focus:border-[--color-accent]"
    oninput="f(this.value)"
  />
  <div id="results" class="mt-3 grid sm:grid-cols-2 lg:grid-cols-3 gap-3"></div>
</div>
